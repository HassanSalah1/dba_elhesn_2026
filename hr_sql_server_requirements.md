# 📋 المتطلبات الفنية وقواعد البيانات من طرف SQL Server
## المرحلة الثالثة - نظام الموارد البشرية (HR Module - Mobile & Web App)

**تاريخ الإعداد:** 29 يوليو 2026  
**مقدم إلى:** البشمهندس كريم (Eng. Karim)  
**الهدف:** تجهيز الـ Views والجداول (Tables) المطلوبة في قاعدة بيانات **SQL Server** لإتمام الربط والمزامنة (Integration) مع تطبيق الهاتف والمنظومة (Laravel / MySQL).

---

## 1. شاشات الحضور والانصراف (Attendance & Work Schedule)
تغطي هذه الجزئية شاشات:
1. **حضور وانصراف الموظفين:** الفلترة بقسم/فئة الموظف، اسم الموظف، والتاريخ.
2. **سجل الحضور الشهري للموظف:** عرض تقويم شهري تفاعلي بحالات الحضور والغياب والإجازات مع النسبة المئوية التقريبية.

### 🔹 1.1. View: فئات الموظفين / الأقسام (Employee Categories)
لتغذية قائمة اختيار فئة الموظف (مثل: الإدارة، الأجهزة الفنية، إداريين...).
* **الاسم المقترح:** `MobileApp_HR_EmployeeCategories`
* **الحقول المطلوبة:**
  * `CategoryID` *(int, Primary Key)*: المعرف الفريد للفئة / القسم.
  * `CategoryNameAR` *(nvarchar(150))*: اسم الفئة بالعربية.
  * `CategoryNameEN` *(nvarchar(150))*: اسم الفئة بالإنجليزية.
  * `Active` *(bit)*: حالة الفئة (`1` = مفعل، `0` = غير مفعل).

### 🔹 1.2. View: بيانات الموظفين (HR Employees)
لربط الموظفين بحسابات التطبيق (`MobileApp_Users`) وبالأقسام التابعين لها.
* **الاسم المقترح:** `MobileApp_HR_Employees`
* **الحقول المطلوبة:**
  * `EmployeeRowID` *(int, Primary Key)*: المعرف الفريد للموظف في نظام HR.
  * `UserID` *(int, Foreign Key)*: رقم الحساب المرتبط بالموظف في جدول `MobileApp_Users`.
  * `CategoryID` *(int, Foreign Key)*: معرف القسم/الفئة التابع لها (`CategoryID`).
  * `NameAR` *(nvarchar(150))*: اسم الموظف بالعربية.
  * `NameEN` *(nvarchar(150))*: اسم الموظف بالإنجليزية.
  * `JobTitle` *(nvarchar(150))*: المسمى الوظيفي (اختياري).
  * `Photo` *(nvarchar(500))*: مسار أو رابط صورة الموظف (إن وجد).

### 🔹 1.3. View: سجلات الحضور والانصراف اليومية (Daily Attendance Records)
لعرض أيام الحضور، الغياب، وتوفير البيانات لرسم النسبة المئوية لحضور الموظف في الأرشيف الشهري.
* **الاسم المقترح:** `MobileApp_HR_AttendanceRecords`
* **الحقول المطلوبة:**
  * `RowID` *(bigint, Primary Key)*: معرف سجل الحضور.
  * `EmployeeRowID` *(int, Foreign Key)*: معرف الموظف (`EmployeeRowID`).
  * `AttendanceDate` *(date)*: تاريخ היום (مثال: `2026-03-18`).
  * `CheckInTime` *(time / datetime)*: وقت التوقيع / الحضور.
  * `CheckOutTime` *(time / datetime)*: وقت الانصراف.
  * `Status` *(varchar(20) / tinyint)*: حالة اليوم البرمجية:
    * `1` أو `Present`: حاضر (يظهر بالأخضر ✓).
    * `2` أو `Absent`: غائب (يظهر بالأحمر ✗).
    * `3` أو `Leave`: إجازة / إذن (يظهر بالبرتقالي).
    * `4` أو `Holiday`: عطلة رسمية.
  * `Notes` *(nvarchar(250))*: ملاحظات أو سبب الغياب/التأخير (اختياري).

---

## 2. شاشات الإجازات والتقارير الإدارية (Leave Requests & Reports)
تغطي هذه الجزئية شاشات:
1. **إضافة طلب إجازة:** تحديد تاريخ الإجازة، نوع الإجازة، وصف داعم، ورفع ملف مرفق.
2. **أرشيف طلبات الإجازة وتفاصيلها:** استعراض الطلبات السابقة للموظف وحالاتها وتاريخ إضافتها.

### 🔹 2.1. View: أنواع الإجازات المتاحة (Leave Types)
لتغذية قائمة اختيار "النوع" في شاشة إضافة الإجازة (مثل: مرضي، سنوي، طارئ، دراسية، بدون راتب، أمومة، حج...).
* **الاسم المقترح:** `MobileApp_HR_LeaveTypes`
* **الحقول المطلوبة:**
  * `TypeID` *(int, Primary Key)*: معرف نوع الإجازة.
  * `TypeNameAR` *(nvarchar(150))*: اسم النوع بالعربية.
  * `TypeNameEN` *(nvarchar(150))*: اسم النوع بالإنجليزية.
  * `Active` *(bit)*: حالة التفعيل (`1` = مفعل، `0` = غير مفعل).

### 🔹 2.2. Table / Staging: جدول طلبات الإجازة (Leave Requests Table)
*يستخدم هذا الجدول لاستقبال طلبات الإجازة الجديدة التي يضيفها الموظف عبر التطبيق (Push from App)، وكذلك لقراءة حالة الطلب (`Approved` / `Rejected`) من نظام الـ HR لعرضها في الأرشيف.*
* **الاسم المقترح:** `MobileApp_HR_LeaveRequests`
* **الحقول المطلوبة:**
  * `RequestID` *(bigint, Primary Key / App ID)*: معرف الطلب المرسل من الموبايل.
  * `EmployeeRowID` *(int, Foreign Key)*: معرف الموظف صاحب الطلب.
  * `TypeID` *(int, Foreign Key)*: معرف نوع الإجازة المختار.
  * `StartDate` *(date)*: تاريخ بداية الإجازة.
  * `EndDate` *(date)*: تاريخ نهاية الإجازة.
  * `Description` *(nvarchar(max))*: حقل "وصف داعم" / تفاصيل أو سبب الإجازة.
  * `AttachmentUrl` *(nvarchar(500))*: رابط أو مسار ملف المرفق الداعم (PDF/PNG/JPG).
  * `Status` *(varchar(50) / tinyint)*: حالة الطلب في الأرشيف:
    * `0` أو `Pending`: قيد المراجعة.
    * `1` أو `Approved`: مقبول.
    * `2` أو `Rejected`: مرفوض.
  * `CreatedAt` *(datetime)*: تاريخ ووقت إنشاء الطلب في التطبيق.
  * `AdminReplyNotes` *(nvarchar(500))*: تعليق أو ملاحظات المسؤول عند الرفض أو القبول (اختياري).

---

## 3. شاشات أرشيف المستندات الإدارية (Administrative Documents)
تغطي هذه الجزئية شاشات:
1. **أرشيف وتفاصيل المستندات:** استعراض كافة التقرير والمستندات المرتبطة بالموظف (تقرير الأداء، الخطط، العقود...).
2. **إضافة مستند:** شاشة لرفع وتخزين مستند جديد بوصف داعم وملف مرفق.

### 🔹 3.1. Table / Staging: جدول أرشيف ومستندات الموظف (HR Employee Documents)
يستعمل لتخزين واستعراض المستندات الإدارية للموظفين في الأرشيف.
* **الاسم المقترح:** `MobileApp_HR_Documents`
* **الحقول المطلوبة:**
  * `DocumentID` *(bigint, Primary Key)*: معرف المستند.
  * `EmployeeRowID` *(int, Foreign Key)*: معرف الموظف أو الجهة المعنية بالمستند.
  * `Description` *(nvarchar(max))*: "وصف المستند" / العنوان الرئيسي الذي يظهر في قائمة الأرشيف والتفاصيل (مثل: *تقرير الأداء السنوي، خطة التسويق للفصل الأول...*).
  * `AttachmentUrl` *(nvarchar(500))*: رابط أو مسار الملف المرفق الذي يمكن تحميله أو تصفحه.
  * `CreatedAt` *(datetime)*: تاريخ إضافة المستند (مثال: `2027-01-15`).

---

## 💡 ملاحظات فنية للمزامنة (Integration Notes)
1. **طريقة نقل الطلبات (Push Sync):**  
   عند قيام الموظف بإضافة طلب إجازة جديد أو رفع مستند عبر التطبيق، يقوم سيرفر اللارافل (MySQL) بالاتصال المباشر بقاعدة بيانات SQL Server وعمل **INSERT** في جدولي `MobileApp_HR_LeaveRequests` و `MobileApp_HR_Documents`.
2. **القراءة التلقائية (Pull Sync):**  
   يقوم تطبيق الموبايل بسحب بيانات الحضور (`MobileApp_HR_AttendanceRecords`) وأنواع الإجازات والفئات بشكل تلقائي للعرض المباشر والدقيقة للموظف والإدارة.
